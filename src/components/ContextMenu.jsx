import React from "react";
import styled from "styled-components";

const ContextMenuContainer = styled.div``;

const ContextMenu = () => {
    return <ContextMenuContainer>x</ContextMenuContainer>;
};

export default styled(ContextMenu)``;
